'use strict';

/**
 *  banner-seccion controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::banner-seccion.banner-seccion');
