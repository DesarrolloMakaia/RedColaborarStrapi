'use strict';

/**
 * page-redcolaborar service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::page-redcolaborar.page-redcolaborar');
