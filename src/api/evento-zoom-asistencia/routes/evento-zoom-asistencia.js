'use strict';

/**
 * evento-zoom-asistencia router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::evento-zoom-asistencia.evento-zoom-asistencia');
