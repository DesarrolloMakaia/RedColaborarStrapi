'use strict';

/**
 * evento-zoom-asistencia service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::evento-zoom-asistencia.evento-zoom-asistencia');
