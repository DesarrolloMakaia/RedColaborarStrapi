'use strict';

/**
 * evento-asistencia service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::evento-asistencia.evento-asistencia');
