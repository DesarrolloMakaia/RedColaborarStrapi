'use strict';

/**
 * recurso-formativo service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::recurso-formativo.recurso-formativo');
