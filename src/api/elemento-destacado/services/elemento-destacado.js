'use strict';

/**
 * elemento-destacado service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::elemento-destacado.elemento-destacado');
