'use strict';

/**
 * tipo-organizacion service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::tipo-organizacion.tipo-organizacion');
