'use strict';

/**
 * recurso-formativo-tipo-contenido service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::recurso-formativo-tipo-contenido.recurso-formativo-tipo-contenido');
