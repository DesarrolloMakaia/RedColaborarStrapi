'use strict';

/**
 * evento-plataforma service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::evento-plataforma.evento-plataforma');
