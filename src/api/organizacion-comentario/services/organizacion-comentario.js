'use strict';

/**
 * organizacion-comentario service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::organizacion-comentario.organizacion-comentario');
