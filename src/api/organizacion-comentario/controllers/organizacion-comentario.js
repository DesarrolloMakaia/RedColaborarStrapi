'use strict';

/**
 *  organizacion-comentario controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::organizacion-comentario.organizacion-comentario');
